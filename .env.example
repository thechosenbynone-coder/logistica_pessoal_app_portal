# Frontend env (Vite)
#
# Demo mode (no backend / no database):
VITE_MOCK_MODE=true
#
# If you later run the backend, disable mock mode and point to the API:
# VITE_MOCK_MODE=false
# VITE_API_BASE_URL=http://localhost:3333
